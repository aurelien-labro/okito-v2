export default function TenantsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Tenants</h1>
      <p className="mt-1 text-sm text-stone-500">Gestion multi-tenant — à implémenter.</p>
      <div className="mt-6 rounded-lg border border-dashed border-stone-300 bg-white px-6 py-12 text-center text-sm text-stone-500">
        Bientôt : liste des tenants, ajout/suppression, configuration des features (voice,
        reminders, deposits…), routing par numéro WhatsApp.
      </div>
    </div>
  );
}
